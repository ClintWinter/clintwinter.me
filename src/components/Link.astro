---
const props = Astro.props;
---
<a {...props} class="font-semibold bg-clip-text text-transparent bg-gradient-to-r from-sky-500 to-green-500"><slot /></a

><style>
a {
    position: relative;
}

a::before {
    @apply bg-gradient-to-r from-sky-500 to-green-500 h-1;
    content: '';
    clip-path: inset(0 100% 0 0);
    position: absolute;
    inset: 0;
    top: auto;
    transition: clip-path 200ms ease-in-out;
    z-index: -1;
}

a:is(:hover, :focus)::before {
    clip-path: inset(0);
}
</style>
